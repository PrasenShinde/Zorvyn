# busy_timeout reduces SQLITE_BUSY errors when reads/writes overlap (e.g. create + list).
DATABASE_URL="file:./dev.db?busy_timeout=5000"
JWT_SECRET="change-me-in-production"
PORT=3000
NODE_ENV=development
